function getChange(coins, moneyAmount, coinAmount) {
    if (moneyAmount > 0) {
        let total;
        coins.sort(function(a,b){return b-a});
        total = coins[0] % 2;
        moneyAmount = total;
        

    }
}